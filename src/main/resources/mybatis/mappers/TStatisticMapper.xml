<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsc.blog.mapper.TStatisticMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zsc.blog.entity.TStatistic">
        <id column="id" property="id" />
        <result column="article_id" property="articleId" />
        <result column="hits" property="hits" />
        <result column="comments_num" property="commentsNum" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, article_id AS articleId, hits, comments_num AS commentsNum
    </sql>

</mapper>
